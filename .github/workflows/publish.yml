name: Publish to PyPI

on:
  push:
    tags: ["humanizer-humanize-v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to publish (e.g. humanizer-humanize-v0.4.0)"
        required: true

permissions:
  contents: read

jobs:
  build-and-publish:
    name: Build + publish humanizer-humanize to PyPI
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/humanizer-humanize/
    # Trusted Publishing to PyPI: https://docs.pypi.org/trusted-publishers/
    permissions:
      id-token: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Verify package version matches tag
        working-directory: humanizer-humanize
        env:
          DISPATCH_TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          VERSION=$(python -c "from scripts import __version__; print(__version__)")
          TAG="${GITHUB_REF_NAME:-$DISPATCH_TAG}"
          EXPECTED="humanizer-humanize-v${VERSION}"
          echo "Tag: ${TAG}"
          echo "Expected: ${EXPECTED}"
          if [ "${TAG}" != "${EXPECTED}" ]; then
            echo "Tag/version mismatch. Bump scripts/__init__.py or retag."
            exit 1
          fi

      - name: Build sdist + wheel
        working-directory: humanizer-humanize
        run: python -m build

      - name: Validate built artifacts
        working-directory: humanizer-humanize
        run: twine check dist/*

      - name: Publish to PyPI (trusted publisher)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: humanizer-humanize/dist/
